# Render backend env template (Railway MySQL over public TCP)
#
# IMPORTANT:
# - Do NOT commit real secrets to git.
# - For Render, set these values in the Render Dashboard (Environment).
# - For local testing, copy to backend/.env and fill in.

# Database (Railway public proxy endpoint)
DB_HOST=metro.proxy.rlwy.net
DB_PORT=15008
DB_NAME=railway
DB_USER=root
DB_PASS=__REPLACE_WITH_YOUR_RAILWAY_PASSWORD__

# Optional: enable if your provider requires TLS
# DB_SSL=true

# Server
PORT=5000
NODE_ENV=production

# Security (use a strong secret, 32+ chars)
JWT_SECRET=__REPLACE_WITH_A_STRONG_SECRET__

# Outbound email (optional)
# If you don't set these, emails are logged to the console.
#
# Common SMTP examples:
# - Gmail (App Password): EMAIL_HOST=smtp.gmail.com, EMAIL_PORT=465, EMAIL_SECURE=true
# - Mailtrap: EMAIL_HOST=sandbox.smtp.mailtrap.io, EMAIL_PORT=587, EMAIL_SECURE=false

EMAIL_HOST=mail.wyzegroup.ng
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=support@wyzegroup.ng
EMAIL_PASS=__REPLACE_WITH_YOUR_SMTP_PASSWORD__
EMAIL_FROM="WYZE Support" <support@wyzegroup.ng>

# Optional branding
EMAIL_BRAND_NAME=WYZE Support
# Use a public URL (recommended) OR a file inside frontend/public (e.g. wyze-logo.jpg)
# EMAIL_BRAND_LOGO_URL=https://example.com/wyze-logo.png
# EMAIL_BRAND_LOGO_FILE=wyze-logo.jpg

# Used for links in emails
PORTAL_URL=https://__YOUR_FRONTEND_DOMAIN__